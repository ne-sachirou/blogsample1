<!DOCTYPE html>
<html>
<head>
  <title>Blogsample1</title>
  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>
</head>
<body>
<header id="header">
  <nav>
    <% if user_signed_in? %>
      Logged in as <strong><%= current_user.email %></strong>.
      <%= link_to "Logout", destroy_user_session_path, method: :delete %>
    <% else %>
      <%= link_to "Login", new_user_session_path %>
    <% end %>
  </nav>
  <h1>Blogsample1</h1>
</header>
<div id="main" class="pure-g">
  <article id="content" class="pure-u-3-4">
    <%= yield %>
  </article>
  <div id="sidebar" class="pure-u-1-4">
    <nav class="pure-menu pure-menu-open">
      <ul>
        <% ItemCategory.all.each do |item_category| %>
          <li><%= link_to item_category.name, item_category %></li>
        <% end %>
      </ul>
    </nav>
  </div>
</div>
</body>
</html>
